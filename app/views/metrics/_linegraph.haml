-if group_by.present?
    = line_chart data.group(group_by).group_by_day(time).count, id: "index-line-graph"
- else
    = line_chart data.map { |d| {name: d[:title], data: d[:data].group_by_day(time).count}}, id: "index-line-graph"