- content_for :title, @newsletter.title
- content_for :newsletter, true

= render 'partials/toast'

%section.section
  .container
    %h3.title.is-3.is-spaced
      = @newsletter.title

    .content
      = @newsletter.content
