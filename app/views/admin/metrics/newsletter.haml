- content_for :title, 'Newsletters Overview'

%section
  .container
    %h1.title.text-center Newsletter Statistics

    .columns
      .column
        %datepicker{ '@set-dates': 'setDates' }

      .column
        .select.float-right
          %select#graph-select{ 'v-model': 'graphOption' }
            %option Newsletter Subscription by Date
            %option Unsubscription by Newsletter
            %option Unsubscription Reason

    %chartkick{ 'v-bind': '{ dates, graphOption }' }
