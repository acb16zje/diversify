- content_for :title, 'Explore Projects'

%section.section.pt-4
  .container
    .title-holder
      %h4.title.is-4.my-4 Explore Projects
      - if user_signed_in?
        = link_to 'New Project', new_project_path, class:'button is-success ml-auto'

    %project-list{ 'init-projects-html': @html,
                   ':init-total': @pagy.count,
                   ':has-tabs': false }
